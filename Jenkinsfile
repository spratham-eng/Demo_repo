pipeline {
    agent any

    stages {
        stage('Checking git logs') {
            steps {
                echo 'Hello World git log'
                sh 'git log'
            }
        }
    }
}
